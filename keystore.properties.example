# Copy to keystore.properties (gitignored) and fill in with the values you chose
# when running keytool. storeFile is resolved against the repository root, so an
# absolute path is fine and is what you want if the keystore lives outside the repo.
storeFile=/Users/YOU/machine-charades-upload.jks
storePassword=
keyAlias=upload
keyPassword=
